{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": ["input.txt", "128", "512"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: gcc build active file"
        }
    ]

}